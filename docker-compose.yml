version: '3'

services:
  api:
    build: .
    ports:
      - 50051:50051
    volumes:
      - ./janken:/go/src/janken
  dynamodb:
    image: amazon/dynamodb-local:latest
    ports:
      - 8000:8000
